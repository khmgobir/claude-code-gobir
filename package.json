{
  "name": "mls-attendance-udus",
  "version": "1.0.0",
  "private": true,
  "description": "MLS Attendance UDUS — Android wrapper (Capacitor)",
  "scripts": {
    "cap:sync": "cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  }
}
